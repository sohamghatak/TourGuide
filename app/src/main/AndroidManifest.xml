<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tourguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ContentActivity"
            android:label="@string/category_content"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AttractionActivity"
            android:label="@string/category_attraction"
            android:parentActivityName=".ContentActivity" />
        <activity
            android:name=".HistoricalActivity"
            android:label="@string/category_historical"
            android:parentActivityName=".ContentActivity" />
        <activity
            android:name=".CuisineActivity"
            android:label="@string/category_cuisine"
            android:parentActivityName=".ContentActivity" />
        <activity
            android:name=".ThingsActivity"
            android:label="@string/category_things_to_do"
            android:parentActivityName=".ContentActivity"></activity>
    </application>

</manifest>